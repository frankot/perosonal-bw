---
title: Frank Web Development
description: Frank Web Developer simple, responsive and fast websites from scratch
---
<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    {% if description %}
    <meta name="description" content="{{description}}" />
    {% endif %}
    <title>{{ title }}</title>

    <link href="https://unpkg.com/prismjs@1.20.0/themes/prism-okaidia.css" rel="stylesheet">
    <link rel="stylesheet" href="/style.css?v={% version %}" />

</head>

<body class="bg-main  text-gray-900 font-custom antialiased    flex flex-col ">
    <div class="min-h-screen">
        {% include components/nav %}
        <div class=" ">
            {% block content %}
            {{ content | safe }}
            {% endblock %}
        </div>
    </div>
    {% include components/footer %}

    <script src="/js/alpine.js?v={% version %}"></script>
    <script>
        let is_mobile = false; // 
        console.log(`is_mobile = ` + is_mobile);
        let is_scrolled = window.pageYOffset > 5 ? true : false;
    </script>
</body>

</html>